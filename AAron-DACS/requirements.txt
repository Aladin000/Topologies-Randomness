# Python dependencies for the analysis scripts in analysis/.
# The simulator itself is pure Java and has no Python requirements.
#
# Install with:  pip install -r requirements.txt

pandas>=2.0,<3.0
numpy>=1.24,<3.0
matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0
